# 007 返佣导航平台 / cryptorebate / INDEX

## 核心目标
把返佣业务做成可视化、可追踪、可拆分的系统，而不是一堆黑盒链接。

## 核心指标
- 点击量
- 注册量
- 首次入金用户数
- 首次交易用户数
- 返佣金额
- 按交易所拆分收益
- 按来源渠道拆分收益
- 按落地页拆分收益
- ROI / LTV 粗估
- 链接素材维度表现（plain text / banner / CSV 导出后使用效果）

## API 设计原则
- 返回字段可读
- 命名统一
- 支持分页 / 过滤 / 时间范围
- 对未来 App 和内部后台友好

## 建议接口
### `GET /api/exchanges`
返回交易所基础信息、返佣比例、标签、适用条件、更新时间

### `GET /api/rebates`
返回返佣规则与展示信息

### `GET /api/stats`
返回点击、注册、转化概览

### `GET /api/earnings`
返回收益数据（日 / 周 / 月）

### `GET /api/materials`
返回推广资产：开户链接、plain text 文案、banner 素材、导出地址

## 数据与同步规则
- 每个交易所单独维护同步任务，按小时或更长周期拉取返佣数据；早期不追求实时到秒，先追求稳定和可追溯。
- 归因字段至少记录：`utm_source`、`utm_medium`、`utm_campaign`、`referrer`、`page_url`、`clicked_at`。
- 建立访问日志与收益表之间的可关联字段，保证“点击 → 注册 → 交易 → 返佣”能尽量串起来。
- 高频看板查询优先用缓存或聚合视图，不要把线上数据库硬查到冒烟。
- 关键统计字段建立索引，避免后期增长后报表变慢。

## 后台看板目标
一眼看懂：
- 哪个交易所最赚钱
- 哪个渠道最赚钱
- 哪个页面转化最高
- 哪种素材最有效
- 哪块内容在白干